# ─────────────────────────────────────────────────────────────
# NetAlerte CM — Frontend : variables d'environnement
# Copie ce fichier en ".env.local" (dev) — jamais commité, voir .gitignore.
# En production (Vercel), cette variable se configure dans le dashboard :
# Project → Settings → Environment Variables — pas dans un fichier ici.
# ─────────────────────────────────────────────────────────────

# URL de base de l'API backend, avec le préfixe /v1.
VITE_API_URL=http://localhost:3000/v1
